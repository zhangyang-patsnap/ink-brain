# Framework Labs

Each child directory is an independent future runtime. No Lab is implemented in the first site release.

Before changing a manifest status to `available`, add:

- isolated dependency manifest
- verified local start command
- health and metadata endpoints
- predefined run scenarios
- SSE terminal events
- cancellation and timeout behavior
- tests and safe secret handling
