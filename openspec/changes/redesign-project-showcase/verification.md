# 验证记录

## 首页直接阅读项目

- 项目首页改为左侧项目选择、右侧 C 风格完整详情；左侧选择时不离开首页。原独立详情链接保留，无 JavaScript 时可直接访问。
- 默认首项、URL 参数选择、刷新恢复、浏览器前进后退、类型筛选后选中项回退、键盘切换、手机列表自动收起验证通过。
- 20 组屏宽（320/390/768/1024/1440）× 纸面/工作台 × 深浅色检查通过。无页面横向溢出、无页面脚本错误。
- 只挂载一份活动正文，重复标题目录目标唯一；切换及刷新后 Mermaid 正常生成且不重复，图表放大/Escape/焦点恢复通过。
- 空项目、单项目、长名称、多资源入口、代码、表格及无 JS 链接降级通过。类型检查、13 项单元测试、静态构建、OpenSpec 严格校验通过。
- 边界案例只在隔离 preview 的 project-workspace-check 路由中，不包含在源码发布路由内；本次未触发正式发布。

## 15:24 卡片右上角补齐

- 按用户确认补回右上角竖分隔线与小字信息。取真实技术栈前三项，缺失时使用项目类型和状态，不引入虚构文案。
- 桌面右侧栏与类型顶部、底部详情入口右边缘对齐；手机将标题放到下一行，保留右上角信息，不与长标题重叠。
- 16 组屏宽/主题/深浅色的可见性、边线、边缘及无溢出断言通过；桌面和手机截图检查通过，无页面脚本错误。类型检查、构建与 diff 检查通过。
- 隔离预览项目数据同步到现有 revision 23，便于检查用户实际“开发文档规范”卡片；没有修改源码项目数据或触发正式发布，active.json 仍为 revision 23。

## 15:05 C 模板校正复验

- 直接对照原始 C 模板，修正此前不一致的左右拼接首卡、单行标题、缺失分类及详情额外色条。
- 首卡始终整色且跨全宽，其余双列；真实 type 筛选支持键盘、选中状态、结果播报，无 JS 展示全部卡片。
- 48 项主题/屏宽/页面组合检查通过，另验证首卡全宽、双列同排、筛选后首卡重排及顺序、目录目标、禁用 JS 和无页面脚本错误。
- 类型检查、构建、12 项单元测试、OpenSpec 严格检查通过。桌面与手机截图已对照检查。
- 本轮没有执行发布操作或写入 active.json。复验时正式指针已由其他操作更新为 revision 21（4adfc107-f33e-4ccf-a62d-76dd70884b23）；不覆盖该并行变更。4332 仍为隔离预览。

## 首轮实现验证（14:53）

2026-09-09，C「作品展台」项目首页及详情落地。

- `npm run check`：0 errors、0 warnings。
- `node --test apps/web/test/*.test.mjs`：12/12 通过，含 4 项项目文档/资源测试。
- `npm run build`：正式源码静态构建通过。现有 Mermaid 大包提示仍存在，本次未新增依赖。
- `openspec validate redesign-project-showcase --strict`：通过。
- Chrome 浏览器 58 项布局检查：纸面/工作台 × 深浅色 × 320/390/768/1440 px，项目首页、真实详情、复杂 README；另含 0–4 张卡片手机/桌面案例。无横向页面溢出、无页面脚本错误。
- 四个有效资源入口、多地址去重、空 README、长标题、代码/表格局部滚动、图片及 Mermaid 已验证。
- 禁用 JavaScript 的手机/桌面目录锚点均可跳转，手机目录键盘展开。Mermaid 点击放大、Escape 关闭及焦点恢复通过。
- 暗色图表沿用 neutral SVG，项目页及放大视图为其保留浅色底，避免箭头/文字失去对比。卡片强调色正文对比度：纸面浅/深 4.63/5.95，工作台浅/深 6.29/9.62。

预览位于 `http://127.0.0.1:4332/projects/`。边界数据仅在临时预览目录 `project-check/`，未写入项目数据或生产路由。正式 `.inkbrain/active.json` 保持 revision 18、release `5cb4e838-4e58-4a92-8a4a-708884d4fa0f`。未发布后台草稿、未提交代码、未归档变更。
## 2026-09-09 原图结构复刻复验

- 以用户 `codex-clipboard-17d3c865-e0fd-4483-91cb-a1435816c058.png` 为依据重新实现右侧纸面详情。恢复浅色标题带、类别与右上竖线小字、标题/介绍/就近状态、三入口及提示、四栏元信息、介绍/能力/文档和右侧带当前项标记的目录；不再以旧版纯分栏近似作为验收。
- 870px 同宽隔离示例截图 `/tmp/project-c-reference-comparison.png` 已查看，与原图逐区对照；主标题实测 47.85px。没有将参考示例内容写入项目数据。实际项目截图 `/tmp/project-c-restored-final.png`，手机截图 `/tmp/project-c-restored-mobile.png`（最后微调前）。视觉结构已对齐；原图为生成位图，未宣称像素差为零。
- `npm run check`：52 文件，0 errors / warnings / hints；`npm run build`：24 页成功，仅现有 Mermaid 包大小提示。
- Web 单元测试 14/14，后台测试 6/6，包括 license/languages 可选、校验、编辑 API 持久化，以及原有发布集成。新增元信息经 publisher 的现有 record.data 路径输出。
- 浏览器：真实缺省项目有四栏资料和三个禁用资源槽位；完整项目三个有效外部入口；切换/刷新/历史/键盘/筛选、唯一 ID、目录当前项、Mermaid 渲染及点击放大/Escape、无 JavaScript 旧路由降级全部通过，无脚本错误。
- 320/390/768/1024/1440 × 纸面/工作台 × 深浅色共 20 组长标题和复杂 README 检查无页面横向溢出。最后字号微调再次检查 320/390/1024/1440。
- 已更新 4332 隔离预览。正式 `.inkbrain/active.json` 保持本轮初始 release `441ba888-e4e5-4cf3-b219-6a176b3403a5` / revision 23；没有发布、修改真实内容或提交代码。后台两个新字段需要运行中的 Node 后台进程重启加载 schema 后再使用；为避免打断用户写作，本轮未重启进程。
## 2026-09-09 等宽双展台修正

- 按本轮完整参考图 `codex-clipboard-303fff24-c7e8-4ca7-b499-9e8b66ed746b.png` 修正：左侧不是导航栏，而是完整 C 项目首页，与右侧详情桌面 1:1 等宽。
- 实测 1600px 窗口：左 754px、右 754px，顶部同为 125px；首卡宽 712px 等于左栏卡片网格宽；后两张普通卡片等宽且同排。左侧两行主标题、横向分类、红色首卡、普通双列卡、右上竖线小字及模板引语均恢复。
- 实际数据桌面截图 `/tmp/projects-equal-real.png`、多项目截图 `/tmp/projects-equal-multiple.png`、手机截图 `/tmp/projects-equal-mobile.png` 均已查看。
- 24 组（320/390/768/1024/1440/1920 × 两套主题 × 深浅色）复杂 README 和长标题检查无页面溢出，桌面两栏始终实测相等；键盘选择、刷新/历史、横向类型筛选后首卡重排、唯一 ID、Mermaid 放大/Escape、手机选择折叠、空/单项目及无 JS 路由降级通过，无页面脚本错误。
- 类型检查 52 文件 0 错误/警告；Web 单元测试 14/14；源码静态构建 24 页及隔离构建通过，保留原 Mermaid 体积提示；OpenSpec 严格校验和 diff 空白检查通过。
- 仅更新 4332 隔离预览；正式指针保持本轮初始 `de522efb-1b23-440c-bd3a-dad383eeee6e` / revision 23。未发布、未重启后台、未提交代码或覆盖项目数据。
